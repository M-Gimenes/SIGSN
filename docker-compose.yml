services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: sigsn-backend
    ports:
      - "3333:3333"
    volumes:
      - ./backend:/app
      - /app/node_modules
    restart: unless-stopped
